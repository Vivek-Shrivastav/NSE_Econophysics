services:
  - type: web
    name: nse-econophysics
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python nse_dashboard.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
